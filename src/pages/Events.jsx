// import { Button } from './components/common';

function Events() {
  return (
    <div
      style={{
        display: 'flex',
        flexDirection: 'row',
        justifyContent: 'center',
        alignItems: 'center',
        alignContent: 'center',
        width: '85%',
        height: '100%',
      }}
    ></div>
  );
}

export default Events;
